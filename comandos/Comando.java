package comandos;

public interface Comando {
    void ejecutar();
    /*Interfaz es un componente de la poo y se trata de una plantilla que permite preesteblecer el contenido y/o
    comportamiento de una o varias clases. Se establece un conjunto de reglas que las clases que impementen la interfaz
    deberan seguir. En nuestro caso, todas las clases tendran que tener el método ejecutar();*/
}
